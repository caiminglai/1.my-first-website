# 同物异名（科学不装）-- 项目总览

> 项目路径：`E:\website\1.my-first-website`
> 线上地址：https://your_domain.com/twym/
> 更新日期：2026-07-04

---

## 文档索引

| 文档 | 内容 |
|------|------|
| [代码参考](./代码参考.md) | 前后端架构、目录结构、数据库设计、关键模块、依赖、错误码 |
| [部署运维](./部署运维.md) | 服务器信息、部署命令、Nginx配置、SSL、问题排查、调试铁律 |
| [API接口文档](./API接口文档.md) | 全部 REST API 端点、请求/响应格式 |
| [README](./README.md) | 项目简介、快速启动（GitHub 首页） |

---

## 项目愿景

建立"认知基础设施"——将复杂世界翻译成人能理解的结构。核心目标：知识平民化、通俗化，把专业术语翻译成日常用语，反对语言障碍与权威壁垒。

核心洞察：不是词条数量，而是"发现不同领域在重复同一种结构"。例如控制论的"负反馈"、经济学的"市场调节"、生物的"内稳态"，本质都属于"系统自稳定"。

### 四大模块

1. **科学不装（同物异名网站）**：把专业术语翻译成"说人话"，覆盖 10 大学科，已收录约 1,069 个词条
2. **概念抗体**：三步还原法（物理/数学/权利还原），识别五类忽悠，引导认知升级
3. **行业分析**：认知地图、看门狗经济学、鬼话拆解、检测工具
4. **岗位拆解**：已收录约 26 个高薪岗位 SOP，降低职业信息黑箱

---

## 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 前端框架 | React + TypeScript | 19.2 / 5.9 |
| 前端构建 | Vite | 7.2 |
| CSS | Tailwind CSS | 3.4 |
| UI 组件 | shadcn/ui (Radix UI) | - |
| 可视化 | D3.js + Framer Motion | 7.9 / 12.40 |
| 后端 | Express.js | 4.19 |
| 数据库 | better-sqlite3 (SQLite 3) | 9.6 |
| 搜索引擎 | Meilisearch | 1.48.3 |
| AI | OpenAI SDK | 4.47 |
| 部署 | Alibaba Cloud Linux + Nginx + PM2 | Node v20.20.2 |

---

## 目录结构

```
E:\website\1.my-first-website\
├── app/                    # React 前端
│   ├── src/
│   │   ├── api/           # API 客户端封装 (client.ts, services.ts)
│   │   ├── components/    # React 组件 (含 ui/ 基础组件)
│   │   ├── contexts/      # React Context
│   │   ├── hooks/         # 自定义 Hooks
│   │   ├── pages/         # 页面组件
│   │   ├── sections/      # 页面区块 (Navbar, Hero, Footer 等)
│   │   ├── types/         # TypeScript 类型
│   │   ├── data/          # 静态数据
│   │   ├── lib/           # 工具库
│   │   └── test/          # 测试文件
│   └── package.json
├── backend/               # Express 后端
│   ├── db/                # 数据库层 (入口.js, 词条.js, 图谱.js, schema.sql)
│   ├── routes/            # 路由定义
│   ├── services/          # 业务逻辑 (AI, 缓存, Meilisearch)
│   ├── middleware/         # 中间件 (安全, 日志)
│   ├── utils/             # 工具函数 (校验, 缓存键)
│   ├── public/            # 管理后台静态页面
│   ├── bin/               # meilisearch.exe
│   └── server.js          # 入口文件
├── data/                  # 数据库文件
│   ├── 同物异名.db        # 主数据库
│   ├── 学科词库.db        # 学科词库
│   ├── 语义词典.db        # 语义词典
│   └── backups/           # 数据库备份
├── meili_data/            # Meilisearch 索引数据
├── docs/                  # 项目文档
└── scripts/               # 根目录工具脚本
```

---

## 快速启动

```bash
# 终端 1：后端
cd E:\website\1.my-first-website\backend
npm install
npm start
# → http://localhost:3000

# 终端 2：前端
cd E:\website\1.my-first-website\app
npm install
npm run dev
# → http://localhost:3001

# 终端 3：Meilisearch（可选，搜索功能需要）
cd E:\website\1.my-first-website\backend\bin
.\meilisearch.exe --db-path ../meili_data --master-key=你的密钥
# → http://localhost:7700
```

本地访问：
- 前端：http://localhost:3001/twym/
- 后端 API：http://localhost:3000/api/v1
- 管理后台：http://localhost:3000/admin

---

## 数据库概览

| 数据库 | 表数量 | 记录数 | 说明 |
|--------|--------|--------|------|
| 同物异名.db | 24 | 17,206 | 主数据库：词条、学科、图谱关系、概念对比、情景还原、岗位等 |
| 学科词库.db | 19 | 8,198 | 学科专业术语词库 |
| 语义词典.db | 3 | 8,598 | 语义关系词典 |

数据库设计原则：文件即数据库（直接读写磁盘文件），WAL 模式，中文表名列名，启动时自动迁移缺失列。

---

## npm scripts

### 前端 (app/)

| 命令 | 说明 |
|------|------|
| `npm run dev` | 开发模式（热重载） |
| `npm run build` | 生产构建（TypeScript 编译 + Vite 打包） |
| `npm run lint` | ESLint 代码检查 |
| `npm run typecheck` | TypeScript 类型检查 |
| `npm run test` | 运行测试 (vitest) |
| `npm run format` | Prettier 格式化 |

### 后端 (backend/)

| 命令 | 说明 |
|------|------|
| `npm start` | 生产环境启动 |
| `npm run dev` | 开发模式（自动重启，需 Node 18+） |
| `npm run backup` | 数据库备份 |
| `npm run import:语义` | 导入语义词典数据 |
| `npm run import:学科` | 导入学科词库数据 |
| `npm run sync:to-obsidian` | 数据库同步到 Obsidian |
| `npm run sync:from-obsidian` | Obsidian 同步到数据库 |
| `npm run sync:meilisearch` | 全量同步词条到 Meilisearch |
| `npm test` | 运行测试 (vitest) |
