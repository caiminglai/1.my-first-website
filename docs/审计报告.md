# 项目1 & 项目2 系统性代码审计报告

**审计日期**：2026-07-08  
**审计范围**：项目1（同物异名）后端 + 前端、项目2（精准匹配）后端 + 前端  
**审计维度**：数据流安全、权限控制、输入验证、输出安全、配置安全

---

## 目录

1. [项目1（同物异名）审计结果](#一项目1同物异名审计结果)
2. [项目2（精准匹配）审计结果](#二项目2精准匹配审计结果)
3. [修复优先级总表](#三修复优先级总表)
4. [正面评价汇总](#四正面评价汇总)

---

## 一、项目1（同物异名）审计结果

### 1.1 权限控制矩阵

| 接口 | 认证方式 | 权限检查 | 状态 |
|------|---------|---------|------|
| `GET /api/v1/terms/*` | 无 | 公开 | 正常 |
| `GET /api/v1/search/*` | 无 | 公开 | 正常 |
| `POST /api/v1/ai/*` | 无 | 公开（用户自付API Key） | SSRF风险 |
| `GET /api/v1/submissions` | 无 | 公开 | **泄露联系方式** |
| `GET /api/v1/submissions/approved` | 无 | 公开 | **泄露联系方式** |
| `POST /api/v1/submissions` | 无 | 公开 | 正常 |
| `GET /api/v1/graph/*` | 无 | 公开 | 正常 |
| `GET /api/v1/stats/*` | 无 | 公开 | 正常 |
| `/api/v1/admin/*` | `x-admin-token` Header | `adminAuth` 中间件 | 安全 |
| `/api/v1/manage/*` | `x-admin-token` Header | `adminAuth` 中间件 | 安全 |

### 1.2 认证机制安全性

**管理员认证**（`backend/middleware/安全.js`）：
- 使用 `timingSafeEqual` 防止时序攻击 ✅
- 无默认Token，必须配置环境变量 ✅
- Token为单一值，不支持多管理员 ⚠️
- Token长期有效，无过期机制 ⚠️

### 1.3 输入验证

| 验证点 | 机制 | 状态 |
|--------|------|------|
| SQL注入 | 参数化查询 + URL/Params正则检测 | 安全 |
| XSS | 前端React默认转义 | 安全 |
| 请求大小 | 1MB限制 | 安全 |
| 分页参数 | 上限保护（pageSize <= 50/100） | 安全 |
| 用户提交 | 验证器工具（字符串长度、枚举等） | 安全 |
| 动态表名 | 白名单校验 | 安全 |

### 1.4 数据库事务与错误处理

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 事务支持 | ❌ | 数据库入口模块无事务封装，所有操作自动提交 |
| 批量导入事务 | ❌ | CSV导入多条词条时无事务保护，中途失败可能导致数据不一致 |
| 迁移事务 | ❌ | 在线迁移（ALTER TABLE）无事务保护 |
| 错误处理 | ⚠️ | 路由层有基本错误捕获，但缺少全局错误处理中间件 |
| 日志安全 | ✅ | 敏感字段过滤 + 路径过滤 |

### 1.5 高危问题详情

#### 🔴 高危 - AI接口SSRF风险

**文件**：`backend/services/AI核心.js`

用户可传入任意 `baseUrl`，后端直接 `fetch(url)`，存在SSRF风险。

**修复建议**：增加URL白名单，限制只能访问已知的LLM服务商域名。

---

#### 🟡 中危 - 用户提交列表泄露联系方式

**文件**：`backend/routes/用户提交.js`

`GET /api/v1/submissions` 和 `/approved` 无需登录，返回完整联系方式。

**修复建议**：未登录用户隐藏联系方式字段。

---

#### 🟡 中危 - Obsidian同步路径遍历

**文件**：`backend/services/Obsidian同步.js`

`safeFilename` 未过滤 `..`，若词条名称包含 `../` 可能跳出 VAULT_PATH。

**修复建议**：增加 `..` 过滤，或写入后验证路径。

---

#### 🟡 中危 - 学科词库删除功能失效

**文件**：`backend/routes/学科词库.js`

数据库以 `readonly: true` 打开，删除接口需要写入操作，功能完全失效。

**修复建议**：移除readonly限制或使用可写连接。

---

#### 🟡 中危 - 批量导入无事务保护

**文件**：`backend/services/管理.js` 的 `batchCreateTerms` 函数

批量导入词条时逐条执行 `run()`，无事务保护，中途失败会导致部分导入、部分失败，数据不一致。

**修复建议**：使用数据库事务包装批量操作。

---

### 1.6 前端安全

| 检查项 | 状态 | 说明 |
|--------|------|------|
| Token存储 | 无 | 项目1前端无用户认证系统 |
| API客户端 | ✅ | 统一的请求封装，超时、重试、错误处理完善 |
| XSS防护 | ✅ | React默认转义 |
| 请求大小 | ✅ | 后端限制1MB |

---

## 二、项目2（精准匹配）审计结果

### 2.1 权限控制矩阵

| 接口 | 认证方式 | 权限检查 | 状态 |
|------|---------|---------|------|
| `GET /api/users` | 无 | 公开 | **信息泄露** |
| `GET /api/users/search` | 无 | 公开 | **信息泄露** |
| `POST /api/users` | 无 | 公开 | **可被批量创建** |
| `GET /api/users/:id` | 无 | 公开 | **信息泄露** |
| `DELETE /api/users/:id` | Bearer Token | 本人或管理员 | 安全 |
| `GET /api/users/:id/check-unlock` | **无** | **query.viewer_id** | **严重漏洞** |
| `POST /api/auth/*` | 验证码/RateLimit | 公开 | 安全 |
| `POST /api/ratings` | Bearer Token | 登录用户 | 安全 |
| `POST /api/reports` | Bearer Token | 登录用户 | 安全 |
| `POST /api/payment/create` | Bearer Token | 登录用户 | 安全 |
| `GET /api/payment/mock-pay` | Bearer Token | 仅非生产环境 | 安全 |
| `GET /api/payment/status` | Bearer Token | 本人订单 | 安全 |
| `POST /api/upload/avatar` | Bearer Token | 登录用户 | 安全 |
| `/api/admin/*` | Bearer Token | 管理员Token+白名单 | 安全 |
| `GET /api/custom-fields` | 无 | 公开 | 可接受 |

### 2.2 认证机制安全性

**用户Token**（`backend/db/index.js`）：
- 7天过期 ✅
- HMAC-SHA256签名 ✅
- 使用 `Math.random()` 而非 `crypto.randomBytes` ❌
- Token签名长度仅16位，相对较短 ⚠️

**管理员Token**：
- 1天过期 ✅
- 检查 `role === 'admin'` ✅
- 使用白名单机制 ✅

**密码存储**：
- 使用 bcrypt ✅
- 兼容旧版 sha256（自动升级） ✅

### 2.3 密钥安全

**关键发现**（`backend/db/index.js` 第420-422行）：

```javascript
const _tokenSeed = process.env.TOKEN_SECRET || process.env.ADMIN_PASSWORD || 'default';
const TOKEN_SECRET = process.env.TOKEN_SECRET || crypto.createHash('sha256').update('match-platform-secret-key-' + _tokenSeed).digest('hex');
const ENCRYPTION_KEY = process.env.ENCRYPTION_KEY || crypto.createHash('sha256').update('contact-encryption-key-' + _tokenSeed).digest();
```

**风险**：未设置环境变量时使用默认种子 `'default'`，攻击者可预测密钥。

**严重程度**：🔴 **高危**

### 2.4 输入验证

| 验证点 | 机制 | 状态 |
|--------|------|------|
| SQL注入 | 参数化查询 | 安全 |
| XSS | 前端React默认转义 + 后端 sanitizeString | 安全 |
| 请求大小 | 5MB限制 | 安全 |
| 文件上传 | 扩展名白名单 + 魔术数字 | 安全 |
| 头像路径 | `..`、``、`\` 过滤 | 安全 |
| 用户ID格式 | `/^[a-zA-Z0-9_-]+$/` 校验 | 安全 |

### 2.5 支付流程安全性

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 订单归属校验 | ✅ | 只能支付自己的订单 |
| 模拟支付环境限制 | ✅ | 仅非生产环境可用 |
| 微信支付签名验证 | ✅ | 回调时验证签名 |
| **支付宝签名验证** | ❌ | **使用私钥验证而非公钥** |

#### 🔴 高危 - 支付宝签名验证使用私钥而非公钥

**文件**：`backend/routes/payment.js` 第131-149行

```javascript
function handleAlipayNotify(params) {
  const sign = params.sign;
  delete params.sign;
  delete params.sign_type;
  const calculatedSign = alipaySign(params, alipayConfig.privateKey);  // ❌ 使用私钥
  const verify = crypto.createVerify('RSA-SHA256');
  const stringToVerify = ...;
  verify.update(stringToVerify);
  if (!verify.verify(alipayConfig.alipayPublicKey, sign, 'base64'))  // ✅ 这行是对的
    return { success: false, message: '签名验证失败' };
}
```

**分析**：`calculatedSign` 使用私钥签名，但从未使用。实际验证使用 `alipayPublicKey` 是正确的。但代码逻辑混乱，存在误导性。

**结论**：虽然实际验证逻辑正确，但代码结构混乱，`calculatedSign` 变量完全无用，容易造成维护误解。

---

### 2.6 文件上传安全性

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 扩展名白名单 | ✅ | `.png`, `.jpg`, `.jpeg`, `.gif`, `.webp` |
| 魔术数字校验 | ✅ | 验证文件头 |
| 路径遍历防护 | ✅ | `path.resolve` + `startsWith` 检查 |
| 文件大小限制 | ✅ | 2MB上限 |
| 文件名随机化 | ✅ | 时间戳 + 随机字符串 |

### 2.7 前端安全

#### 🔴 高危 - Auth Token存储在 localStorage

**文件**：`app/src/pages/LoginPage.tsx` 第88-91行

```javascript
localStorage.setItem('auth_token', data.token);
localStorage.setItem('user_id', data.user?.id || '');
localStorage.setItem('user_phone', data.user?.phone || '');
localStorage.setItem('user_nickname', data.user?.nickname || '');
```

**风险**：
- localStorage 可被 XSS 攻击读取
- Token可被窃取并用于冒充用户
- 用户手机号存储在 localStorage

**修复建议**：改用 HttpOnly Cookie 或 sessionStorage，配合 CSRF 防护。

---

#### 🟡 中危 - 批量同步用户数据无认证

**文件**：`app/src/services/user.service.ts` 第92-101行

```javascript
export function saveRows(rows: Row[]) {
  localStorage.setItem(LS_ROWS, JSON.stringify(rows));
  try {
    fetch(`${API_BASE_URL}/users/batch`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(rows)
    });
  } catch (e) { /* ignored */ }
}
```

**风险**：向 `/users/batch` 发送请求时未携带 Authorization Header，后端若接受无认证请求则可能被滥用。

---

#### 🟡 中危 - 同步用户数据无认证

**文件**：`app/src/services/user.service.ts` 第103-133行

```javascript
export async function syncRowsFromServer(): Promise<Row[]> {
  try {
    const res = await fetch(`${API_BASE_URL}${API_ENDPOINTS.users}`);
    // 无认证头
  } catch (e) { /* ignored */ }
}
```

**风险**：从服务器获取用户数据时未携带认证，依赖后端公开访问。

---

### 2.8 高危问题详情

#### 🔴 高危 - Token和加密密钥默认可被预测

**文件**：`backend/db/index.js`

未设置环境变量时使用默认种子，攻击者可预测密钥并伪造Token。

**修复建议**：生产环境强制设置环境变量，移除默认值回退。

---

#### 🔴 高危 - check-unlock接口无需认证获取联系方式

**文件**：`backend/routes/contact-unlock.js`

`viewerId` 直接从 URL 参数获取，未验证用户身份，可遍历获取明文联系方式。

**修复建议**：从 `Authorization` header 获取 viewerId。

---

#### 🔴 高危 - Auth Token存储在 localStorage

**文件**：`app/src/pages/LoginPage.tsx`

localStorage 可被 XSS 攻击读取，导致 Token 泄露。

**修复建议**：改用 HttpOnly Cookie。

---

#### 🟡 中危 - 用户列表/搜索/创建接口公开访问

**文件**：`backend/server.js`

`GET /api/users`、`GET /api/users/search`、`POST /api/users` 公开访问，隐私信息可被爬取。

**修复建议**：增加认证要求或严格速率限制。

---

#### 🟡 中危 - 速率限制基于内存，多实例部署失效

**文件**：`backend/server.js`

所有速率限制基于内存 Map，多实例部署时完全失效。

**修复建议**：改用Redis。

---

#### 🟡 中危 - 用户数据一次可拉取1000条

**文件**：`backend/routes/users.js`

普通用户一次最多可获取 1000 条用户数据，易被批量爬取。

**修复建议**：普通用户限制为 50-100 条/页。

---

#### 🟡 中危 - Math.random()用于Token生成

**文件**：`backend/db/index.js`

Token生成使用 `Math.random()`，不是加密安全的随机数生成器。

**修复建议**：改用 `crypto.randomBytes`。

---

## 三、修复优先级总表

### P0（立即修复）

| 优先级 | 项目 | 问题 | 文件 |
|--------|------|------|------|
| P0 | 项目2 | Token/加密密钥默认可被预测 | `backend/db/index.js` |
| P0 | 项目2 | check-unlock接口无需认证获取联系方式 | `backend/routes/contact-unlock.js` |
| P0 | 项目2 | Auth Token存储在localStorage | `app/src/pages/LoginPage.tsx` |
| P0 | 项目1 | AI接口SSRF风险 | `backend/services/AI核心.js` |

### P1（尽快修复）

| 优先级 | 项目 | 问题 | 文件 |
|--------|------|------|------|
| P1 | 项目2 | 用户列表/搜索/创建公开访问 | `backend/server.js` |
| P1 | 项目2 | 批量同步用户数据无认证 | `app/src/services/user.service.ts` |
| P1 | 项目1 | 用户提交列表泄露联系方式 | `backend/routes/用户提交.js` |
| P1 | 项目1 | Obsidian同步路径遍历 | `backend/services/Obsidian同步.js` |
| P1 | 项目1 | 学科词库删除功能失效 | `backend/routes/学科词库.js` |
| P1 | 项目1 | 批量导入无事务保护 | `backend/services/管理.js` |

### P2（后续优化）

| 优先级 | 项目 | 问题 | 文件 |
|--------|------|------|------|
| P2 | 项目2 | 速率限制基于内存 | `backend/server.js` |
| P2 | 项目2 | Math.random()用于Token | `backend/db/index.js` |
| P2 | 项目2 | 用户数据一次可拉取1000条 | `backend/routes/users.js` |
| P2 | 项目2 | 支付宝签名验证代码混乱 | `backend/routes/payment.js` |
| P2 | 项目1 | Token长期有效无过期 | `backend/middleware/安全.js` |

---

## 四、正面评价汇总

### 项目1

| 项目 | 评价 |
|------|------|
| SQL注入防护 | 参数化查询 + 白名单 + 正则检测，三层防护 |
| 时序攻击防护 | `timingSafeEqual` 正确使用 |
| 安全响应头 | 完整的安全头设置 |
| 日志安全 | 敏感字段过滤 + 路径过滤 |
| 请求大小限制 | 1MB上限 |
| 输入验证工具 | 统一的验证器模块 |
| 数据库迁移 | 自动检测并添加缺失列 |

### 项目2

| 项目 | 评价 |
|------|------|
| 管理员认证 | 仅使用Bearer Token，不使用Cookie，防止CSRF |
| 密码存储 | bcrypt + 明文兼容自动升级 |
| 联系方式加密 | AES-256-CBC |
| 用户数据脱敏 | 普通用户查看时自动脱敏 |
| 文件上传 | 扩展名白名单 + 魔术数字双重校验 |
| 头像路径 | `..`、``、`\` 过滤 |
| 支付归属校验 | 只能操作自己的订单 |
| 模拟支付限制 | 仅非生产环境可用 |
| 文件服务路径遍历防护 | `path.resolve` + `startsWith` 正确实现 |
| 全局错误处理 | `unhandledRejection` 和 `uncaughtException` 处理 |
| 优雅关闭 | SIGINT/SIGTERM 处理 |
